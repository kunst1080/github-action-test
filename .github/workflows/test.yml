name: workflow-test

on:
  push:
    #paths:
    #- aaa/**
    branches:
    - master

jobs:
  job1:
    runs-on: ubuntu-latest
    name: docker build test
    steps:
      - name: step 1
        uses: docker://alpine:3.8
        with:
          args: uname -a
  job2:
    runs-on: ubuntu-latest
    name: native runner test
    steps:
      - name: step 1
        shell: bash
        run:
          args: |
            echo "This is step 1"
      - name: step 2
        shell: bash
        run:
          args: |
            echo "This is step 2"
