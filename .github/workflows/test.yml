name: hoge

on:
  push:
    #paths:
    #- aaa/**
    branches:
    - master

jobs:
  build:
    runs-on: ubuntu-latest
    name: docker build test
    steps:
      - name: step 1
        uses: docker://alpine:3.8
        with:
          args: uname -a
